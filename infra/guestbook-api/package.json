{
  "name": "guestbook-api",
  "version": "1.0.0",
  "description": "Guestbook submission API — stdlib only, no dependencies",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
